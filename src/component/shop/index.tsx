import React from "react";
import { Typography } from "@mui/material";
import Layout from "../layout";

const Shop: React.FC = () => {
  return (
    <Layout>
      <Typography variant="h1">Shop</Typography>
      <Typography variant="body1">
        This is the content of the Shop page.
      </Typography>
    </Layout>
  );
};

export default Shop;
